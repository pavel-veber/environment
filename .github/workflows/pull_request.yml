name: Rebuild all images 

on: [pull_request]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@master

    - name: Install buildah
      run: |
        sudo apt update
        sudo apt install software-properties-common uidmap -y
        sudo add-apt-repository ppa:projectatomic/ppa -y
        sudo apt update
        sudo apt install -y podman buildah python3-pip
        sudo pip3 install setuptools ansible

    - name: Build images
      run: ansible-playbook -i ./inventory/hosts ./build.yaml -vv
